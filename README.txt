ИСПРАВЛЕНИЕ ДАННЫХ И СТАТУСА МОБИЛЬНОГО ПРОФИЛЯ

Заменить файлы с сохранением папок.

Исправлено:
- имя и Email берутся из регистрации/Firebase;
- марка, модель и год сохраняются при регистрации;
- поле «Автомобиль» собирается из марки, модели и года;
- статус «Профиль подтверждён» показывается только при реальном user.emailVerified === true;
- при неподтверждённой почте показывается «Почта не подтверждена»;
- статус Firebase обновляется через user.reload();
- поле телефона остаётся обычным полем профиля.
